# --- Inference provider (image-to-image restyle only) ---
REPLICATE_API_TOKEN=
RESTYLE_MODEL=
RESTYLE_PRICE_PER_IMAGE_USD=
RESTYLE_POLL_TIMEOUT_SECONDS=
RESTYLE_JOB_TIMEOUT_SECONDS=

# --- Upload validation ---
MAX_UPLOAD_SIZE_BYTES=
ALLOWED_IMAGE_FORMATS=

# Optional merchant styling notes (disabled by default)
# MAX_EXTRA_STYLING_LEN=

# --- Restyle policy ---
MAX_RESTYLE_VARIATIONS=
IMAGE_SIZE=
OUTPUT_IMAGE_FORMAT=

# --- Storage ---
STORAGE_BACKEND=
LOCAL_STORAGE_DIR=
S3_BUCKET=
S3_REGION=

# --- DB / queue ---
DATABASE_URL=
REDIS_URL=
RQ_QUEUE_NAME=

# --- HTTP behavior towards Replicate ---
REQUEST_TIMEOUT_SECONDS=
MAX_RETRIES=
RETRY_BACKOFF_SECONDS=

# --- DB connection pool ---
DB_POOL_SIZE=
DB_MAX_OVERFLOW=
DB_POOL_RECYCLE_SECONDS=